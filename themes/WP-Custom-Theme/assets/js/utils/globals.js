import $ from 'jquery';

export const $win = $(window);
export const $doc = $(document);
export const $body = $('body');
export const $main = $('.main');
export const $header = $('.header');
export const $adminBar = $('#wpadminbar');
export const hasFixedHeader = true;
